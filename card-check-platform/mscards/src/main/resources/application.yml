spring:
  application:
    name: mscards
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 0 #use a random port


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka # Eureka server to register
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}} # Generate a unique name for each instance of this ms
